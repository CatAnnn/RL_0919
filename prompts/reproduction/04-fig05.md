# 四、图5：三个MG的训练奖励曲线

> 按需模块：生成或审计三个微电网的真实训练奖励曲线。
> 与根目录 `prompt.md` 的全程约束一起使用。

输出一张包含三个纵向子图的图：
(a) MG1
(b) MG2
(c) MG3

横轴：本地训练迭代；
纵轴：Reward。

标注：
Phase 1、Phase 2、Phase 3；
500和1000处的实际聚合事件。

同时保存：
episode_reward = sum(r_t)
discounted_return = sum(gamma**t * r_t)

论文没有完整交代图中奖励汇总和平滑口径，
必须明确主图使用哪个指标。
不能混用单步奖励、训练累计奖励和测试奖励。

所有曲线必须来自真实训练日志。
保留原始奖励；平滑仅用于显示，不得覆盖数据。
平滑窗口不能跨联邦边界制造假改善或掩盖突变。

图中的Iteration/Reward标注必须从实际日志读取。

原文提及的MG1第二阶段约-15059.85、
MG2第二阶段约-3524.99只能作为报告中的参考值，
不能作为人工设定的终点或奖励缩放目标。
