# 仓库贡献指南

## 项目结构

当前仓库包含论文复现提示词和参考论文，尚未实现 Python/PyTorch 实验项目。

- `prompt.md`：复现入口与全程约束。
- `prompts/reproduction/`：按编号组织的复现任务模块及使用说明。
- `prompts/reproduction/templates/reproduction_state.md`：进度模板；开始实际复现时复制到根目录并填写。
- 根目录 PDF：主要科学依据，保留原文件。

`envs/`、`agents/`、`tests/`、`data/` 等属于后续交付目录，不应声称已经存在。

## 环境与开发命令

所有 Python 执行、依赖安装、测试、训练和评估统一使用 Conda 环境 `pytorch_env`。在仓库根目录的 PowerShell 中执行：

```powershell
conda activate pytorch_env
python -c "import sys, torch; print(sys.executable); print(torch.__version__)"
rg --files -g '*.md'
Get-Content -LiteralPath 'prompt.md' -Raw -Encoding UTF8
```

以上命令分别激活环境、核对解释器与 PyTorch 版本、列出文档和读取入口。无法激活时可使用 `conda run -n pytorch_env python ...`；环境缺失时报告问题，不静默切换环境。安装依赖使用该环境中的 `python -m pip`。

当前没有可执行的训练入口、自动化测试框架或格式化工具；实现后补充经过验证的运行命令。

## 提示词加载顺序

先读取根目录 `prompt.md`，再按以下顺序加载 `prompts/reproduction/` 中的任务文档。完整复现时按阶段推进，避免一次性加载全部正文。

1. `01-paper-spec.md`：论文规格、假设和歧义。
2. `02-environment.md`：数据与物理环境。
3. `03-algorithms.md`：算法、训练与检查点。
4. `04-fig05.md`：训练奖励曲线。
5. `05-fig06-08.md`：24小时调度结果。
6. `06-fig09.md`：不同检查点的物理指标。
7. `07-fig10.md`：四算法比较与稳定性报告。
8. `08-delivery.md`：项目交付、验证和差异报告。

模块加载顺序以本文件为准，科学约束仍遵循 `prompt.md`。加载顺序不替代执行依赖：完整训练前须按上述顺序预读后续图表与验收要求，确认日志和检查点满足需求。

`README.md` 仅在需要使用帮助时读取，模板仅在建立进度记录时读取。继续已有任务时核验进度和产物，再从当前阶段续接；用户限定单项任务时，只加载相关模块及必要依赖，并保持编号顺序。文档维护本身不触发训练。

## 编码与修改规范

- 文件使用 UTF-8；新增代码注释使用中文，写入后检查中文乱码。
- Python 使用四空格缩进；Markdown 使用清晰标题、相对链接和带语言标记的代码块。
- 模块文件保留两位编号及连字符命名，例如 `06-fig09.md`。
- 聚焦当前功能，尽量不改动已经正确实现的其他功能。
- 修改函数前先理解原逻辑，在原有实现基础上修改，保留有效逻辑。

## 验证与科学真实性

文档修改检查路径、链接、加载顺序、UTF-8 编码，以及公式、参数和约束是否完整。当前没有测试覆盖率指标。

实验实现须通过 `08-delivery.md` 规定的相关单元测试和短程冒烟测试，再开展完整训练。明确区分未执行、执行失败和验证通过，记录工程假设及证据路径，禁止伪造训练日志或实验结果。

## 提交与合并请求

当前目录没有 Git 元数据，无法核实历史提交惯例。后续启用版本控制时，建议使用简短、明确的提交信息，例如 `docs: 明确图9检查点要求`。

合并请求说明修改目的、涉及模块、验证方式和遗留问题；有相关议题时附链接，图形修改附前后对比图。
